# Determinism

Given the same manifest, content hashes, platform capability report, and policy
inputs, Universal Setup should produce the same plan and audit classification.
Network fetch timing and local filesystem layout must not change compatibility
contracts.
