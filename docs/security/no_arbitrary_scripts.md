# No Arbitrary Scripts

Universal Setup must not become a generic script runner. Package recipes and
product manifests are declarative inputs to the setup planner. Platform-specific
helpers must be explicit, reviewed, and audited.
