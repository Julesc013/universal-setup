# Win32 GUI

Optional Windows setup GUI shell. The legacy-friendly implementation can be
WinForms, but this directory is named for the Windows provider boundary rather
than a specific toolkit.

It must call the same setup command model and must not own install logic.
